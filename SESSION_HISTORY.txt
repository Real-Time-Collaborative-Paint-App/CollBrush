CollBrush Session History
Date: 2026-03-04

[1] Request: Build a real-time collaborative paint app (Next.js + TypeScript), multi-user like Miro, max 10 concurrent users per session, multi-session boards, responsive and clean UI.
    Changes made:
    - Scaffolded Next.js (App Router, TypeScript, Tailwind).
    - Added Socket.IO server and realtime board rooms.
    - Implemented max 10 users per board.
    - Added multi-board routing and board create/join flow.
    - Built collaborative canvas UI with pen/eraser/colors/size/clear/copy-link.
    - Updated metadata, styles, scripts, and README.
    - Verified with lint/build and runtime startup.

[2] Request: Add a txt file to store session history and update after every change.
    Changes made:
    - Created this file: SESSION_HISTORY.txt
    - Added initial full history entries for this session.
    - Going forward, this file will be appended after each code/content change in our session.

[3] Request: "project doesnt launch"
    Investigation:
    - Reproduced startup behavior and observed intermittent Next.js lock conflict.
    - Error captured: unable to acquire .next/dev/lock because another dev instance was active.
    Fixes made:
    - Killed duplicate/stale dev instance.
    - Added automatic stale lock cleanup in npm dev script before starting server.
    Result:
    - `npm run dev` launches successfully and serves on port 3000.

[4] Request: 11th user should see "This Board is full" and be rerouted to main page.
    Changes made:
    - Server join-board rejection message changed to exactly: "This Board is full".
    - Board client now checks that rejection reason and redirects to `/?error=board-full`.
    - Home page now reads `error` query param and displays "This Board is full" message.
    Result:
    - Any user above the 10-user limit is denied, redirected to main page, and sees the required error text.

[5] Request: Rework color choosing to use a color palette instead of predefined options.
    Changes made:
    - Removed fixed preset color swatches from the toolbar.
    - Added a color palette picker using `input type="color"`.
    - Picker now sets selected color and switches tool to pen mode automatically.
    Result:
    - Users can choose any color from the palette for drawing.

[6] Request: Add login to distinguish users, show each user's cursor with nickname, and add per-board active user list at top.
    Changes made:
    - Extended realtime protocol with user identity, join request payload, presence users list, and cursor events.
    - Added nickname-based login flow on home page (nickname input + persisted user id/nickname in localStorage).
    - Board route now passes user identity (`uid`, `name`) to the collaborative board page.
    - Server now stores per-board user metadata, emits full presence user lists, and broadcasts cursor positions.
    - Board UI now shows active users list in the header and renders live remote cursors with nickname labels.
    Result:
    - Users are distinguished by nickname, visible to others in presence list and cursor labels in real time.

[7] Request: Ask nickname when creating/joining board; ensure top user list and nickname cursors are visible.
    Changes made:
    - Updated home flow so Join/Create actively prompts for nickname if nickname field is empty.
    - Kept persisted nickname behavior in localStorage for subsequent joins.
    - Confirmed board already has active users list on top and live cursors with nickname labels.
    Result:
    - User is asked for nickname at board entry actions and identity features remain visible on board.

[8] Request: Fix bug when opening existing board via link; add accounting system to stabilize identity.
    Changes made:
    - Added centralized account storage utility (`lib/account.ts`) for persistent user id + nickname.
    - Updated home page to use account utility for nickname restore and navigation identity parameters.
    - Updated board page logic to recover account from link params or localStorage, and prompt nickname only if truly missing.
    Result:
    - Direct board links no longer fail repeatedly with nickname-required error when account data already exists.

[9] Request: Assign random color to each user for cursor and nickname; add random animal emoji (except monkey) next to top user nicknames.
    Changes made:
    - Added user metadata fields for `cursorColor` and `animalEmoji` in realtime protocol.
    - Server now assigns random color + random animal emoji on board join (emoji pool excludes monkey).
    - Board header user chips now show `emoji + nickname` styled with each user's assigned color.
    - Remote cursor dot and cursor nickname label now use each user's assigned color.
    Result:
    - Each user is visually identifiable by random color and animal emoji, without affecting drawing stroke color.

[10] Request: Add emoji icons to each tool and add a new Bucket tool that works like Microsoft Paint fill.
    Changes made:
    - Added tool emoji labels: ✏️ Pen, 🧽 Eraser, 🪣 Bucket.
    - Implemented bucket flood-fill on canvas with contiguous pixel fill behavior.
    - Added realtime `fill-area` event so bucket actions sync instantly to all users.
    - Migrated board history from only segments to a unified action stream (segments + fills) so new users replay full board state correctly.
    Result:
    - Bucket tool works collaboratively like Paint fill and all tools now have emoji icons.

[11] Request: Add Select tool (move/delete selected area) and Color Picker tool like Microsoft Paint.
    Changes made:
    - Added new tools: 🔲 Select and 🎨 Picker in toolbar.
    - Implemented eyedropper behavior: click canvas to pick pixel color and switch back to draw mode.
    - Implemented marquee selection with move support by dragging selected area.
    - Implemented selection delete using keyboard Delete/Backspace.
    - Added collaborative `replace-canvas` action/event so selection move/delete changes sync to all users.
    - Extended action history model to include replace-canvas snapshots alongside draw/fill actions.
    Result:
    - Users can select/move/delete regions and pick colors from canvas, with selection edits synchronized board-wide.

[12] Request: Make selected-area move visible to other users in real-time and smooth.
    Changes made:
    - Added `replace-canvas-preview` realtime event on server for non-persistent live move previews.
    - During selection drag, canvas now updates every pointer move using cached base image + selected image block.
    - Selection movement now emits throttled preview snapshots (~25 FPS) so other users see motion live.
    - Final drop still commits a persistent `replace-canvas` snapshot for canonical board state/history.
    Result:
    - Other users now see selected area moving in real time, with smoother synced movement.

[13] Request: Implement Ctrl+Z and Ctrl+Y/Ctrl+Shift+Z (undo/redo).
        Changes made:
        - Added local snapshot history stacks (`undo`/`redo`) based on before/after canvas data URLs.
        - Bound keyboard shortcuts:
            - Ctrl+Z => undo last local action
            - Ctrl+Y or Ctrl+Shift+Z => redo last undone action
        - Undo/redo actions are synced to all users by broadcasting `replace-canvas` snapshots.
        - Integrated history tracking for draw/erase strokes, bucket fill, selection move, selection delete, and clear.
        Result:
        - Users can revert and re-apply their recent actions with standard keyboard shortcuts.

    [14] Request: Implement Text tool (font/size/color/bold/italic/strikethrough/spoiler) and movable mustard sticky notes.
        Changes made:
        - Added collaborative board object model (`text` and `sticky`) to protocol and join payload.
        - Added server-side object storage and realtime events: `upsert-object` and `remove-object`.
        - Added new tools: 🔤 Text and 🗒️ Sticky.
        - Added Text tool controls for content, font family, size, color, bold, italic, strikethrough, and spoiler mode.
        - Added Sticky input and rendering with mustard-colored background.
        - Added drag-to-move behavior for text and sticky objects with throttled realtime sync for smooth multi-user updates.
        - Fixed build/lint blockers encountered during integration (search params and effect/state lint constraints).
        Result:
        - Users can place styled text, hide text as spoiler, place/move sticky notes, and see all changes collaboratively in real time.

    [15] Request: Edit existing text/stickynotes, resize text/stickynote blocks, remove pre-entry text fields, and make text behave like Paint.
        Changes made:
        - Extended text/sticky object model with `width` and `height` for true resizable blocks.
        - Removed pre-add text/sticky content fields from toolbar.
        - Text tool now creates an editable text box directly on canvas (Paint-style direct text entry).
        - Sticky tool now creates editable mustard note blocks directly on canvas.
        - Added in-place editing for existing text and sticky notes (double-click/edit; blur to finish).
        - Added resize handle for both text and sticky blocks with realtime collaborative sync.
        - Added object selection and keyboard delete support for selected text/sticky objects.
        Result:
        - Existing text/sticky objects can be edited and resized; text entry happens directly in-canvas like Paint.

    [16] Request: Auto-deselect sticky tool after placement; resize handle in bottom-right corner; per-object text toolbar above each selected block; style editing for selected text in both text and sticky blocks.
        Changes made:
        - Sticky tool now automatically switches back to draw mode after placing a sticky note.
        - Resize handle is anchored at the true bottom-right corner of text/sticky block.
        - Added per-object floating text toolbar above selected object (font, size, color, bold, italic, strikethrough, spoiler).
        - Removed remaining global pre-entry fields and global text toolbar pattern.
        - Enabled style changes for both text blocks and sticky note text.
        - Extended sticky object schema with text style fields for collaborative style sync.
        Result:
        - Formatting controls are now object-specific and context-aware, matching requested workflow.

    [17] Request: Add rotate option next to resize for text/stickynotes; create text/sticky by press-hold-drag to set initial size; if text is selected change only selected fragment style (font/size/color), otherwise apply to whole block.
        Changes made:
        - Added `rotation` field to text and sticky object schema and normalization defaults.
        - Implemented rotate handle button beside resize handle with live collaborative rotation sync.
        - Reworked text/sticky placement flow to press-hold-drag-release, including a visible placement preview rectangle.
        - Replaced textarea editing with contenteditable editing for both text and sticky objects.
        - Added selected-text style application (font family, size, color) during editing; falls back to whole-object style update when no text range is selected.
        - Preserved existing per-object formatting toolbar and collaboration broadcasting.
        Result:
        - Text/sticky blocks now support drag-sized creation, per-object rotation, and partial text styling behavior as requested.

    [18] Request: Add drag tool, bind Esc to drag tool, drag text/sticky/selected elements only with drag tool, reveal spoiler on hover, and fix font + selected-text property bugs.
        Changes made:
        - Added new ✋ Drag tool to toolbar and extended tool mode handling.
        - Added Esc keyboard shortcut to switch active tool to Drag instantly.
        - Reworked canvas interaction so moving selected pixel regions works in Drag mode.
        - Restricted text/sticky object dragging to Drag mode only.
        - Added spoiler hover reveal behavior for text and sticky content (hidden by default, revealed on hover).
        - Fixed text toolbar interaction bug by removing blocking pointer default on toolbar container.
        - Added selection range preservation for contenteditable editing so font/size/color changes can apply to selected text even when toolbar controls take focus.
        - Added blur guard to keep edit session active when interacting with toolbar controls.
        Result:
        - Drag behavior now follows dedicated tool semantics, Esc selects drag mode, spoilers reveal on hover, and font/selected-text styling works reliably.

    [19] Request: Deselect selected area on outside click; fix spoiler causing full block black on other users; apply bold/italic/strikethrough/spoiler to selected text only (fallback to whole block when no selection).
        Changes made:
        - Added outside-click deselection for selected canvas regions (except while creating new selection).
        - Updated Drag tool behavior so clicking outside selected area clears selection.
        - Extended selected-text styling engine to support boolean styles: bold, italic, strikethrough, spoiler.
        - Rewired toolbar buttons (B/I/S/Spoiler) to selection-aware toggles with whole-object fallback only when no text selection exists.
        - Added inline spoiler markup class for selected fragments and hover-reveal CSS.
        - Updated object-level spoiler background handling to avoid forced black background when hovered.
        Result:
        - Selected area now deselects correctly on outside click.
        - Spoiler no longer unintentionally blackens full text blocks for remote users when applied to selection.
        - B/I/S/Spoiler now behave like font/size/color: selected text first, whole block only when no selection.

    [20] Request: Make strikethrough not affect text typed after use; reveal spoiler on hover with any tool/plain cursor; use heavy blur for spoiler instead of black fill.
        Changes made:
        - Updated selected-text styling flow to collapse caret after applying inline style spans, preventing style carryover to subsequently typed text.
        - Reworked spoiler rendering from black concealment to blur-based concealment.
        - Added block-level spoiler blur class (`cb-block-spoiler`) with hover reveal.
        - Updated inline selected spoiler class (`cb-inline-spoiler`) to heavy blur with hover reveal.
        - Kept hover behavior tool-agnostic by using hover-based reveal styles on rendered objects.
        Result:
        - New text typed after applying strikethrough to a selected fragment no longer inherits strikethrough unintentionally.
        - Spoiler content is now obscured by heavy blur and revealed on hover regardless of current tool.

    [21] Request: Spoiler should affect only selected text (not whole block); allow dragging selected items with both Select and Drag tools; fix text deletion bug during redaction; make redaction smoother.
        Changes made:
        - Updated Select tool behavior so clicking inside existing selection starts move (same as Drag), while Drag still supports moving selected area.
        - Enabled object drag interactions in both Select and Drag tools.
        - Made spoiler toggle strictly selection-based (no whole-object fallback for spoiler).
        - Refactored selected-text style application to wrap only extracted selection content and reinsert safely (prevents selection-redaction text loss).
        - Applied spoiler wrapping to text nodes within the selected fragment for more stable redaction behavior.
        - Preserved caret repositioning after style application for smoother continued typing/editing.
        Result:
        - Spoiler now applies to selected fragments only.
        - Selected content can be moved with either Select or Drag tool.
        - Redaction no longer deletes text and behaves more smoothly during editing.

        [22] Request: Allow dragging text/sticky with corresponding tool by pressing empty part of block/node; fix bug where text hides during redaction.
                Changes made:
                - Expanded object drag activation to work when matching object tool is active:
                    - Text tool can drag text blocks.
                    - Sticky tool can drag sticky notes.
                    - Existing Select/Drag tool dragging remains supported.
                - Improved inline redaction rendering by adding explicit spoiler data/style markers on wrapped selected text spans.
                - Switched spoiler style rules to global scope so dynamically inserted redaction spans are reliably styled.
                - Tuned spoiler blur intensity and reveal transition to avoid text disappearing while still redacted.
                Result:
                - Text/sticky objects can be moved with their corresponding tools.
                - Redacted text remains visibly blurred instead of appearing fully hidden.

        [23] Request: Add corner drag button for text/sticky blocks; fix spoiler reveal-on-hover bug; allow dragging any element including pen-drawn ones; add tool hotkeys (Q/S/D/E/P) and hover hints.
                Changes made:
                - Added a dedicated drag handle button in object corner that starts drag behavior regardless of selected tool.
                - Added drag-tool support for pen/fill drawings by detecting connected opaque canvas region under cursor and moving it as a selected area.
                - Added hotkeys:
                    - `Q` → Pen
                    - `S` → Select
                    - `D` → Drag
                    - `E` → Eraser
                    - `P` → Picker
                - Added hover tooltips with hotkey hints on the corresponding tool buttons.
                - Fixed spoiler hover reveal by removing inline filter overrides on dynamic spoiler spans and relying on global hover-reveal classes.
                - Aligned sticky edit rendering so spoiler state does not hide text while editing.
                Result:
                - Blocks/notes can be dragged from their corner drag button.
                - Drawn canvas elements can be dragged in Drag mode without pre-selection.
                - Hotkeys and hover hints are available for core tools.
                - Spoiler content correctly reveals on mouse hover.

            [24] Request: Add horizontal/vertical flip buttons for selected areas (including text), add magnifier tool (LMB zoom in / RMB zoom out), add shape picker section with last used shape and drag-to-size outline placement.
                Changes made:
                - Added selection toolbar above active selection with `↔ Flip` and `↕ Flip` buttons.
                - Implemented pixel flip for selected canvas region (horizontal/vertical).
                - Implemented object flip integration for selected region by mirroring intersecting text/sticky objects and toggling per-object `flipX` / `flipY` state.
                - Extended board object protocol with `flipX` and `flipY`, and normalized defaults for backwards compatibility.
                - Added magnifier tool (`🔍 Zoom`) with LMB zoom-in and RMB zoom-out centered around cursor.
                - Applied zoom transform to board stage and blocked context menu in zoom mode.
                - Added Shapes section showing last used shape and dropdown chooser (`Rectangle`, `Ellipse`, `Line`).
                - Added shape outline drawing mode using current color and brush size, with press-and-drag sizing and snapshot commit on release.
                - Added connected-region drag pickup in Drag mode so drawn pen/fill elements can be selected/moved by clicking them.
                - Added/kept tool hover hints for requested hotkeys (`Q/S/D/E/P`).
                Result:
                - Selected areas now provide flip controls and flipped output includes overlay objects in that area.
                - Zoom and shapes workflows are available directly from toolbar.
                - Any drawn region can be grabbed and moved in Drag mode.

        [25] Request: Add more shapes (star, star of David, northern star, arrow, double-pointed arrow), show shape outlines in shape selection UI, add hold-to-zoom behavior with 0.2s delay, and add magnifier hotkey `G`.
                Changes made:
                - Extended shape model and drawing engine with:
                    - Star
                    - Star of David
                    - Northern Star
                    - Arrow
                    - Double Arrow
                - Added outline icon previews for every shape option in the shapes dropdown.
                - Updated shapes button to display current selected shape with its outline icon.
                - Implemented press-and-hold zoom behavior in magnifier mode:
                    - Starts continuous zoom after 0.2 seconds hold.
                    - LMB continuously zooms in.
                    - RMB continuously zooms out.
                    - Stops cleanly on release/cancel/unmount.
                - Added hotkey `G` to switch to magnifier tool.
                - Updated magnifier tooltip hint to include `G` and hold behavior details.
                Result:
                - Shapes palette now includes requested advanced outline shapes with visual previews.
                - Magnifier supports both click-step zoom and continuous hold zoom with requested delay.
                - Keyboard shortcut `G` switches to magnifier tool.

        [26] Request: Add hotkeys for text (`T`), sticky notes (`N`), bucket (`B`), and shapes (`C`).
                Changes made:
                - Added keyboard shortcuts in tool handler:
                    - `B` → Bucket
                    - `T` → Text
                    - `N` → Sticky
                    - `C` → Shapes
                - Updated hover hints/tooltips on corresponding toolbar buttons to show these hotkeys.
                Result:
                - Requested tool hotkeys are active and discoverable via button hover hints.

            [27] Request: Allow scrolling of the shape-choice window.
                Changes made:
                - Updated shapes dropdown container with max height and vertical overflow scrolling.
                Result:
                - Shape-choice menu is now scrollable when options exceed visible area.

            [28] Request: Add scroll-wheel + side-scrollbar support for shape selection window; show shape drawing on other users' screens live while drawing for smooth experience.
                Changes made:
                - Enhanced shape dropdown container to always show vertical scrollbar (`overflow-y-scroll`).
                - Added wheel-handler on shape menu so mouse wheel scrolls the shape list reliably and prevents page/canvas scroll conflicts.
                - Wired shape preview drawing path to emit throttled realtime preview frames while dragging shape size.
                - Reused collaborative preview broadcast channel so other users see in-progress shape outlines before mouse release.
                Result:
                - Shape chooser now scrolls with both mouse wheel and visible side scrollbar.
                - Shape placement appears smoothly in real-time on other connected clients during drag, not only after release.

            [29] Request: Fix scrolling not working in shape selection window; add heart shape.
                Changes made:
                - Reworked shape dropdown scrolling to use native `overflow-y-auto` behavior.
                - Reduced shape menu max height to force predictable overflow and enable reliable wheel/scrollbar interaction.
                - Added new `Heart` shape to shape type model.
                - Implemented heart outline drawing logic in shape renderer.
                - Added heart option to shape dropdown and heart outline icon preview.
                Result:
                - Shape selection window now scrolls reliably with mouse wheel and side scrollbar.
                - Heart shape is available in the shapes tool and renders as outline like other shapes.

        [30] Request: Fix Northern Star outline mismatch with icon in shape selection window; add rotate and resize options to every dragged/selected object.
                Changes made:
                - Updated Northern Star drawing geometry to match the icon polygon coordinates exactly.
                - Added selection-level transform controls alongside flip controls:
                    - Rotate (`↻ Rotate`, 90°)
                    - Resize up (`＋ Size`)
                    - Resize down (`－ Size`)
                - Implemented selected-region pixel transforms for rotate and resize.
                - Applied matching transforms to intersecting text/sticky objects inside selected region (position, rotation, size where applicable).
                - Kept collaborative sync/history behavior by committing transform results via snapshot broadcast and object upserts.
                Result:
                - Northern Star outline now visually matches its icon in the shapes UI.
                - Selected content has rotate/resize options in addition to flip, and text/sticky objects are transformed together with the selected region.

            [31] Request: Close shape selection window when clicking outside; fix selection tool choosing only a small dot instead of area.
                Changes made:
                - Added outside-click detection for the shape menu using a container ref and global pointerdown listener.
                - Shape menu now closes when pointer interaction happens outside its button/dropdown area.
                - Fixed selection tool drag behavior by scoping shape-session preview handling to `shape` mode only.
                - Prevented stale shape-session branch from hijacking pointer move updates when using `select` mode.
                Result:
                - Shape selection dropdown closes on outside click.
                - Select tool correctly updates selection rectangle area during drag instead of staying as a small dot.

        [32] Request: Make selected-area resize/rotate work smoothly like Microsoft Paint and make selected area flip together with selected object.
                Changes made:
                - Replaced step-based selection rotate/resize actions with pointer-drag transform sessions for smooth continuous changes.
                - Added live transform preview for selected pixels while dragging rotate/resize controls.
                - Added live transform updates for intersecting/selected text-sticky objects during selection transforms.
                - Updated selection controls:
                    - `↻ Rotate` now supports smooth drag angle changes.
                    - `↘ Resize` now supports smooth drag scaling.
                - Kept flip controls and extended flip object targeting to include currently selected object (`activeObjectId`) in addition to intersecting objects.
                - Updated Northern Star outline geometry to match icon points more precisely.
                Result:
                - Selection rotate/resize now behaves smoothly during drag rather than discrete jump steps.
                - Flipping selected area now flips the selected object together with the area.

        [33] Request: Add rotate and resize buttons in top-right corner of selection and make behavior more like text block handles.
                Changes made:
                - Moved selection rotate/resize controls from top action strip into handle-style buttons at the selection’s top-right corner.
                - Added compact corner controls matching object handle interaction pattern:
                    - rotate handle (`↻`, drag for smooth angle)
                    - resize handle (corner drag, smooth scaling)
                - Kept existing smooth transform session logic and pointer-capture drag behavior for Paint-like feel.
                Result:
                - Selection transform controls now work visually and interactively like text/sticky object handles.

            [34] Request: Add more padding between selection resize and rotate handles; make selection outline rotate together with selected objects.
                Changes made:
                - Increased spacing between top-right selection rotate and resize controls by moving resize handle farther below rotate handle.
                - Added selection rotation state tracking (`selectionRotation` / `selectionPreviewRotation`).
                - Updated selection overlay rendering to apply rotation transform to the selection outline and corner handles.
                - Wired smooth selection transform session updates to drive rotating outline preview in real time.
                - Persisted final selection rotation after transform completion and reset it appropriately on clear/delete/deselect flows.
                Result:
                - Rotate/resize handles have clearer spacing.
                - Selection outline now rotates together with selected content, matching object-style transform behavior.

            [35] Request: Fix bug where rotating selected shape changes size; make other users see rotation/resizing in real time.
                Changes made:
                - Fixed rotate transform session so selection bounds remain stable during rotate mode (no unintended size/bounds recomputation while rotating).
                - Kept resize mode using dynamic bounds updates, but rotate mode now preserves original selection rect.
                - Added realtime preview emission during selection transform updates so canvas transforms stream to other users while dragging.
                - Added throttled realtime `upsert-object` broadcasting during transform updates so text/sticky object rotate/resize changes are visible to others live.
                - Final selection rect commit logic now respects transform mode to avoid rotate-mode rect drift.
                Result:
                - Rotation no longer causes accidental scaling of selected content.
                - Other collaborators now see in-progress selection rotation/resize changes in real time.

            [36] Request: Ensure heart is added to shapes tool and make dropdown window longer.
                Changes made:
                - Moved `Heart` shape option near top of shapes list for immediate visibility.
                - Increased shape dropdown max height so it extends further down over the board area.
                Result:
                - Heart is clearly visible in the shape selector.
                - Shape dropdown appears longer and drops lower on the board.

            [37] Request: Improve heart shape, restore scrolling in shape dropdown, and show more of dropdown window.
                Changes made:
                - Reworked heart outline rendering to a smoother parametric heart curve for better shape quality.
                - Set shape dropdown to fixed taller height (`h-64`) to display more options at once.
                - Switched dropdown overflow to always show vertical scrollbar (`overflow-y-scroll`) so scroll interaction is clearly available.
                Result:
                - Heart outline looks more natural.
                - Shape selection dropdown is taller and scrollbar-based scrolling is visibly available again.

        [38] Request: Fix missing shapes in selection window and make dropdown appear above board for better visibility.
                Changes made:
                - Removed fixed dropdown height constraint and switched to `max-h-[70vh]` so all shape entries can be reached reliably.
                - Kept vertical scrolling enabled with `overflow-y-auto` for long lists.
                - Raised stacking context:
                    - Header set to `relative z-50`.
                    - Shape dropdown set to `z-[120]` with stronger shadow.
                Result:
                - Shape list no longer appears cut off.
                - Dropdown clearly overlays the board and is easier to see/interact with.

            [39] Request: Place drag button in bottom-left corner of selected area; fix drag tool not working with selected area and text/sticky on first button press.
                Changes made:
                - Added selection drag handle (`✥`) at the bottom-left corner of selected area.
                - Implemented handle-driven selection drag start/move/end flow with live preview and snapshot commit.
                - Updated rotated selection overlay container to `pointer-events-none` so it no longer blocks first-click interactions on canvas.
                - Set transform handles (rotate/resize/drag) to `pointer-events-auto` so controls remain interactive.
                - Fixed object drag first-press behavior by exiting edit mode immediately when drag/select mode is active on the same object.
                Result:
                - Selected area can be dragged from bottom-left corner handle.
                - Drag tool interactions on selected regions and text/sticky objects work correctly on first press.

            [40] Request: Add screenshot tool with options for entire board or selected area, downloading PNG files.
                Changes made:
                - Added a new `📸 Screenshot` tool button with dropdown options:
                    - `🖼️ Entire board`
                    - `⛶ Selected area`
                - Implemented board PNG export that composes current canvas pixels and text/sticky board objects into an offscreen canvas.
                - Added selected-area screenshot mode that switches to selection flow, waits for user drag-select, then auto-downloads only that region as PNG.
                - Added click-outside handling to close screenshot dropdown (alongside existing shape dropdown behavior).
                Result:
                - Users can export the full board to PNG in one click.
                - Users can choose selected-area export, draw a selection box, and receive a cropped PNG download.

            [41] Request: Add a white background to each screenshot.
                Changes made:
                - Updated screenshot render pipeline to fill the export canvas with solid white before drawing board content.
                - Updated selected-area crop export to also initialize cropped output with solid white before drawing cropped pixels.
                Result:
                - All downloaded screenshots (entire board and selected area) now have a white background instead of transparency.

            [42] Request: Add a top-left button that reroutes user to the main menu.
                Changes made:
                - Added a new `⬅ Main menu` button in the board header (top-left area).
                - Wired button click to `router.push("/")` for direct navigation back to the main page.
                Result:
                - Users can return to the main menu from the board with one click.

            [43] Request: Add a button to copy board ID and change the main menu button icon.
                Changes made:
                - Added a new `🆔 Copy board ID` button in the toolbar.
                - Wired the button to copy `boardId` to clipboard via `navigator.clipboard.writeText(boardId)`.
                - Updated main menu button icon from arrow to home icon (`🏠 Main menu`).
                Result:
                - Users can copy the board ID directly with one click.
                - Main menu button now uses a home icon.

            [44] Request: Add image insertion via Ctrl+V and RMB context menu with upload/cut/copy/paste actions and emoji icons.
                Changes made:
                - Added global image paste handling (`Ctrl+V`) that detects image data from clipboard and inserts it onto the board.
                - Added custom right-click (RMB) context menu on the board canvas with emoji actions:
                    - `✂️ Cut`
                    - `📋 Copy`
                    - `📥 Paste`
                    - `🖼️ Upload an image`
                - Implemented image upload flow via hidden file input opened from context menu.
                - Implemented selection copy/cut for canvas region, including browser clipboard write when supported and internal fallback clipboard memory.
                - Implemented paste action that first tries system clipboard image read and falls back to internal copied image data.
                - Inserted/pasted/uploaded images are drawn on canvas, synchronized by snapshot replace, included in undo history, and auto-selected after insertion.
                Result:
                - Users can paste image files/screenshots with `Ctrl+V`.
                - Users can right-click and upload images from computer.
                - Users have cut/copy/paste actions in RMB menu with emoji icons.

            [45] Request: Disallow pasting a link into Board ID input on the main screen.
                Changes made:
                - Added board-link detection helper for home page Board ID input (`http://`, `https://`, `www.`, `/board/`).
                - Added `onPaste` guard on Board ID input to block pasted links and show an inline error.
                - Added join-time fallback validation to reject board-link values even if typed manually.
                - Added inline error message under Board ID input for invalid link input.
                Result:
                - Users can no longer paste board links into Board ID field; only plain board IDs are accepted.

            [46] Request: Add "View my boards" screen with board previews, rename support, max 5 boards per user, and create-limit message.
                Changes made:
                - Added new local board storage utility `lib/boards.ts` for per-user board registry:
                    - stores up to 5 boards per user,
                    - board id,
                    - board name,
                    - updated timestamp,
                    - preview image data URL.
                - Home page updates (`app/page.tsx`):
                    - Added `🗂️ View my boards` button that routes to `/my-boards`.
                    - Added create-board limit enforcement using user board registry.
                    - If user already has 5 boards, create action is blocked and shows:
                      `You have 5 boards already - delete one to create new`.
                    - Successful create now registers the new board in user board storage.
                - Added new page `app/my-boards/page.tsx`:
                    - Shows user boards list with content previews.
                    - Allows opening a board.
                    - Allows renaming board names (edit input, saves on blur).
                    - Allows deleting boards from list.
                - Board editor updates (`components/CollaborativeBoard.tsx`):
                    - Added periodic board preview persistence for tracked boards.
                    - Previews are generated from composed board canvas and saved to user board registry.
                Result:
                - Users can open a dedicated "My boards" list with board previews.
                - Users can rename and delete boards in that list.
                - One user is limited to 5 saved boards; creating a 6th is blocked with the requested message.

            [47] Request: Ensure any created board that user modifies is saved to "My boards".
                Changes made:
                - Added local dirty-state tracking in board editor to detect user-made changes.
                - Board preview persistence now runs only when board has local edits.
                - Removed prior requirement that board must already exist in storage before preview save.
                - Changed preview save to auto-upsert board entry by `boardId` when edited, preserving existing custom name when present.
                - Marked board dirty on:
                    - local canvas/history commits,
                    - local object upserts,
                    - local object removals.
                Result:
                - Any board a user changes is automatically added/updated in "My boards" with current preview.

            [48] Request: Fix board changes not being saved between sessions.
                Changes made:
                - Added server-side persistent storage in `server.ts` using JSON file on disk (`.data/boards.json`).
                - Implemented board state serialization/deserialization for actions + objects.
                - Added startup load of persisted boards so saved board content is available after server restart.
                - Added debounced persistence writes for board mutations:
                    - draw segment,
                    - fill,
                    - replace canvas,
                    - object upsert/remove,
                    - clear board,
                    - board cleanup deletion.
                - Updated empty-room cleanup logic to keep non-empty boards in memory (and persisted), deleting only truly empty boards.
                Result:
                - Board content now persists across sessions/server restarts instead of resetting when in-memory state is lost.

            [49] Request: Fix bug removing all boards from My Boards and board progress after rejoin.
                Changes made:
                - Fixed account persistence in `lib/account.ts`:
                    - `saveAccount` now reuses existing `userId` when `userId` is not provided,
                    - prevents unintended user-id regeneration on routine saves,
                    - preserves existing nickname when new nickname is empty.
                - Hardened server persistence in `server.ts`:
                    - added queued persistence writes,
                    - added explicit flush of pending board writes on socket disconnect,
                    - added process shutdown (`SIGINT`/`SIGTERM`) flush before exit.
                Result:
                - `My Boards` no longer appears to reset due user identity switching.
                - Board progress is much less likely to be lost when leaving/rejoining or restarting server.

            [50] Request: Ensure board progress remains after rejoin and show all users currently using board.
                Changes made:
                - Further hardened server persistence writes to be atomic in `server.ts`:
                    - write to temp file (`.data/boards.json.tmp`) then rename to `.data/boards.json`.
                - Updated board header user section in `components/CollaborativeBoard.tsx`:
                    - added explicit `Online users` label,
                    - renders all active user chips,
                    - shows fallback chip when no users are online.
                Result:
                - Board persistence is more robust against partial/corrupt writes.
                - Active board participants are clearly visible in the UI.

            [51] Request: Update board preview to current state, ask for board name on create, and fix board clearing when everyone leaves.
                Changes made:
                - Home create flow (`app/page.tsx`) now prompts for board name before creating a new board.
                - Saved board entry now uses prompted board name (creation cancels if name prompt is cancelled/empty).
                - Board preview update triggers in `components/CollaborativeBoard.tsx` now mark preview dirty on:
                    - initial board join state load,
                    - incoming draw/fill/replace/object/clear events.
                - This keeps My Boards previews aligned with current collaborative board state.
                - Updated `server.ts` empty-board cleanup behavior to stop deleting board records when users leave.
                Result:
                - New boards are named by user at creation.
                - Board previews stay up to date with latest board content.
                - Boards no longer get cleared simply because everyone disconnected.

            [52] Request: Fix React error about useEffect dependency array size changing between renders.
                Changes made:
                - Updated socket/join synchronization effect in `components/CollaborativeBoard.tsx` to avoid capturing `markBoardDirty` callback in the dependency list.
                - Replaced `markBoardDirty()` calls inside that effect with direct `boardDirtyRef.current = true` assignments.
                - Removed `markBoardDirty` from that effect dependency array.
                Result:
                - Effect dependency array length remains stable across renders/Fast Refresh.
                - Console error about final argument size change is resolved.

            [53] Request: Ensure board progress saves between sessions.
                Changes made:
                - Fixed server persistence write path in `server.ts` for Windows compatibility.
                - Atomic temp-file replace now handles rename collisions (`EEXIST`/`EPERM`) by removing old target file and retrying rename.
                Result:
                - Board state file updates reliably on Windows.
                - Rejoining existing boards after session restart now keeps saved progress consistently.

            [54] Request: Prior persistence fix still not working; explain and fix.
                Root cause identified:
                - Previous write path still depended on temp-file rename success.
                - On Windows, rename/replace behavior can fail in additional lock scenarios, causing persistence to silently fail in practice.
                Changes made:
                - Updated `server.ts` persistence logic to guarantee fallback write:
                    - write temp file first,
                    - try atomic rename,
                    - if rename fails, directly write final file (`boards.json`) as fallback,
                    - cleanup temp file best-effort.
                - Added startup recovery read path:
                    - if loading `boards.json` fails, attempt loading `boards.json.tmp`.
                Result:
                - Board progress persistence now no longer depends solely on rename success.
                - Rejoin/session restore works reliably even under Windows file-lock edge cases.

            [55] Request: Add `Hooray` confetti button with emoji to RMB context menu.
                Changes made:
                - Added new RMB context menu action: `🎉 Hooray`.
                - Implemented animated party popper effect at click location.
                - Added multi-piece confetti burst overlay that shoots across the board area.
                - Confetti and popper animation auto-disappear after a short duration.
                - Added cleanup of pending confetti timeouts on component unmount.
                Result:
                - Right-click menu now includes a celebratory confetti action that visually pops and fades away.

            [56] Request: Add `Sharknado` button to RMB context menu with shark tornado animation.
                Changes made:
                - Added new RMB context menu action: `🦈🌪️ Sharknado`.
                - Implemented animated shark-tornado overlay with swirling shark emojis around a tornado core.
                - Sharknado effect auto-disappears after a short duration and reuses timeout cleanup flow.
                - Increased context menu vertical sizing logic to keep menu inside board viewport with new action item.
                Result:
                - Right-click menu now includes Sharknado effect that creates a temporary small tornado made of shark emojis.

            [57] Request: Fix Sharknado so sharks orbit the tornado.
                Changes made:
                - Reworked shark animation transform chain to use true orbit wrappers.
                - Each shark now rotates around tornado center while its own offset radius/scale is applied in inner element.
                - Removed prior direct translate+rotate drift combination that prevented clean orbital motion.
                Result:
                - Sharks now visibly orbit around the tornado core as intended.

            [58] Request: Rework Sharknado to tornado-like orbit with changing size/visibility.
                Changes made:
                - Expanded shark particle model with staged orbit radii, scale phases, and vertical lift.
                - Rewrote sharknado keyframes into multi-phase spiral motion:
                    - near radius start,
                    - wider mid/far orbit,
                    - narrowing exit,
                    - upward lift over lifetime.
                - Added dynamic opacity and scale transitions across animation phases to create depth/visibility illusion.
                - Increased shark count and staggered timings for denser tornado feel.
                Result:
                - Shark emojis now move in a clear tornado pattern and visually appear to orbit around the tornado emoji with changing size and visibility.

            [59] Request: Change Sharknado from top-view orbit to side-view and increase tornado emoji size.
                Changes made:
                - Reworked shark motion model to side-view tornado lanes:
                    - per-shark vertical lane offset,
                    - horizontal sway path,
                    - upward drift over lifetime.
                - Updated shark keyframes to mimic front/back pass in side profile using opacity and scale phase shifts.
                - Increased tornado core emoji size from `text-3xl` to `text-6xl` and tuned core scale animation.
                Result:
                - Sharknado now reads as a side-view tornado with sharks circulating around the column.
                - Tornado emoji is significantly larger and more prominent.

            [60] Request: Make Sharknado last longer, increase spin diameter, and remove initial static shark.
                Changes made:
                - Increased Sharknado lifetime and per-shark animation duration.
                - Increased shark sway/drift ranges to widen effective spin diameter.
                - Extended tornado core animation duration to match longer effect.
                - Removed static pre-animation shark appearance by:
                    - forcing initial shark opacity to 0,
                    - switching shark animation fill mode to `both` (applies 0% state during delay).
                Result:
                - Sharknado stays visible longer, spins in a wider column, and no longer shows a static shark at start.

            [61] Request: Increase shark spin speed, keep spinning longer, and make some sharks fly far away randomly.
                Changes made:
                - Increased spin speed by reducing per-shark spin duration and stagger delay.
                - Increased overall Sharknado lifetime window and tornado core animation duration.
                - Added new breakaway shark particle set with random vectors, rotations, delays, and distances.
                - Added `cb-sharknado-breakaway` animation for far random launch motion.
                Result:
                - Sharks spin faster while effect remains visible for longer.
                - Additional sharks now peel off and fly far away in random directions.

            [62] Request: Make tornado disappear faster, make sharks 2x faster, and move all emojis randomly but synchronically.
                Changes made:
                - Doubled shark spin speed by approximately halving spin durations and stagger delays.
                - Sped up breakaway shark motion timings accordingly.
                - Made tornado core disappear much faster by shortening core animation duration and reducing lingering end transform.
                - Added synchronized random group wobble animation (`cb-sharknado-wobble`) applied to entire Sharknado burst.
                - Each burst gets random wobble amplitude/speed so all emojis in that burst move together while still feeling organic.
                Result:
                - Tornado emoji fades out faster.
                - Shark emojis spin significantly faster.
                - Whole Sharknado motion now has synchronized random movement closer to a real chaotic tornado.

            [63] Request: Add RMB Coinflip action with animation and result text held for 2 seconds.
                Changes made:
                - Added new RMB context menu action: `🪙 Coinflip`.
                - Implemented animated coin flip overlay (`cb-coinflip-spin`) with randomized Heads/Tails result.
                - Added result badge text (`🪙 Heads` / `🪙 Tails`) with delayed reveal and timed fade-out.
                - Result display is held on-screen for ~2 seconds after flip resolution, then disappears with animation.
                - Updated context menu bounds height to fit the additional action entry.
                Result:
                - Right-click menu now supports coinflip animation with clear result text that auto-disappears after the hold period.

            [64] Request: Make coin spin horizontally and make heads/tails different.
                Changes made:
                - Switched coin flip axis from vertical (`rotateY`) to horizontal (`rotateX`) in coinflip keyframes.
                - Added two distinct 3D coin faces:
                    - front face `H` (heads style),
                    - back face `T` (tails style).
                - Coin now lands on a deterministic final rotation based on random result (`Heads` => 0°, `Tails` => 180°).
                - Updated result badge to visually differentiate outcomes:
                    - `👑 Heads`
                    - `🦅 Tails`
                Result:
                - Coin now flips horizontally and heads/tails are clearly distinct in both animation and result text.

            [65] Request: Make coin spin around center, show result only after spin stops, and use only coin emoji.
                Changes made:
                - Simplified coin visual to single `🪙` emoji (removed custom H/T face layers).
                - Ensured center pivot spinning with explicit centered transform origin.
                - Kept horizontal-axis spin (`rotateX`) and adjusted timing.
                - Delayed result text animation start until after coin spin ends.
                Result:
                - Coin flips as a centered emoji-only animation.
                - Heads/Tails text appears only after spinning completes.

            [66] Request: Make heads/tails truly random, keep axis at center, and remove result text at animation start.
                Changes made:
                - Switched coin result generation to crypto-backed random bit (`crypto.getRandomValues`) with fallback.
                - Restored result-linked final coin rotation (`Heads` => 0°, `Tails` => 180°) for consistent landing behavior.
                - Strengthened center-axis rendering by using fixed centered coin container (`h-16 w-16`, centered transform origin).
                - Forced result badge hidden state before reveal (`opacity: 0`) and set animation fill mode to `both`.
                Result:
                - Heads/Tails outcome is generated from stronger randomness source.
                - Coin rotates around the middle reliably.
                - Result text no longer appears at the beginning of coin animation.

            [67] Request: Make coin stop at exact start orientation and spin vertically.
                Changes made:
                - Switched coin flip animation axis from `rotateX` back to `rotateY` (vertical spin).
                - Removed end-rotation offset logic from coinflip state.
                - Set final keyframe to full-turn multiple (`rotateY(1440deg)`) so coin ends exactly where it started.
                Result:
                - Coin now spins vertically and lands in the exact same orientation as initial frame.

            [68] Request: Keep coin spinning without changing coordinates during animation.
                Changes made:
                - Updated `cb-coinflip-spin` final keyframe to remove vertical translation drift.
                - Coin spin now uses constant position (`translateY(0)` at both start/end).
                Result:
                - Coin rotates in place and no longer shifts coordinates while animating.

            [69] Request: Add bottle tool to RMB menu that spins and stops on a user after random 5-7 turns.
                Changes made:
                - Added new RMB context menu action: `🍾 Bottle`.
                - Implemented bottle burst model with:
                    - active board users mapped to circle participants,
                    - random selected user,
                    - random spin count in range 5-7,
                    - computed final bottle rotation to stop at selected participant.
                - Added overlay rendering:
                    - users (nickname + emoji) positioned around a circle,
                    - bottle emoji in center with directional indicator,
                    - selected user highlight and result label after spin.
                - Added viewport clamping so circle remains visible near edges.
                - Extended context menu positioning height budget for the extra action.
                Result:
                - Right-click menu now supports spin-the-bottle behavior that randomly selects one active user after 5-7 spins.

            [70] Request: Fix bottle not spinning during animation and not centered in the circle.
                Changes made:
                - Replaced mount-time transition approach with explicit keyframe spin animation (`cb-bottle-spin`) using target rotation variable.
                - Separated bottle centering and rotation into nested elements:
                    - outer element handles exact center placement (`translate(-50%, -50%)`),
                    - inner element handles rotation animation only.
                - Kept directional pointer tied to rotating inner group.
                Result:
                - Bottle reliably spins every time.
                - Bottle remains precisely centered within the participant circle.

            [71] Request: Remove arrow from bottle and make bottle neck point to selected user at animation end.
                Changes made:
                - Removed the directional arrow marker from bottle rendering.
                - Recalibrated bottle final rotation using a bottle-neck angle offset, so the bottle emoji neck aligns to the selected user when the spin finishes.
                Result:
                - Spin-the-bottle now ends with the bottle neck (not an arrow) indicating the selected user.

            [72] Request: Highlight chosen user nickname only after spin stops and ensure bottle points to winner nickname at end.
                Changes made:
                - Changed participant badges to use a delayed winner-highlight animation instead of immediate selected styling.
                - Winner highlight now starts after bottle spin duration completes.
                - Kept bottle final angle tied to selected participant angle with bottle-neck offset for end alignment.
                Result:
                - Winner nickname highlight appears only once the bottle has stopped spinning.
                - Bottle lands pointing toward the winner by the end of the spin.

            [73] Request: Fix bottle neck not pointing to nickname at end; allow more than 5-7 turns including partial turns.
                Changes made:
                - Reworked bottle target-angle math to explicitly aim from spin center to selected winner label direction.
                - Updated turn count from fixed integer range to fractional spins (`5 + random*4`), allowing partial extra turns and more natural stop angles.
                - Preserved bottle-neck offset calibration in final angle computation.
                Result:
                - Bottle can spin beyond 5-7 with semi-complete turns.
                - End orientation is now locked to the selected winner direction at animation finish.

            [74] Request: Fix remaining bug where bottle neck still does not point to winner at the end.
                Changes made:
                - Switched to deterministic end-angle targeting using selected participant angle directly.
                - Added normalized target rotation computation to keep final orientation stable across all quadrants.
                - Increased spin range to `6 + random*6` while preserving exact winner-facing terminal angle.
                - Moved neck calibration into a single constant (`BOTTLE_NECK_ANGLE`) for consistent tuning.
                Result:
                - Bottle now ends on a normalized, winner-facing angle with reliable final orientation.

            [75] Request: RMB should only summon context menu (except zoom tool), and middle mouse should pan when zoomed in.
                Changes made:
                - Added input gating so non-zoom tools ignore RMB pointer-down entirely.
                - Kept zoom-tool RMB behavior for zoom-out while suppressing context-menu popup in zoom mode.
                - Added middle-mouse drag panning when `zoomLevel > 1`, updating zoom origin continuously.
                - Updated zoom tool tooltip to document middle-button pan behavior.
                Result:
                - Right mouse button is now reserved for context menu across tools (zoom tool remains the only RMB exception).
                - Users can pan around the board with the scroll-wheel button while zoomed in.

            [76] Request: Fix zoom-out showing more board than drawable area; do not zoom out past 100%.
                Changes made:
                - Updated zoom-step clamping minimum from `0.25` to `1`.
                Result:
                - Zoom-out is now capped at 100%, so users cannot zoom out beyond the drawable board.

            [77] Request: Add an "Are you sure" window after pressing Clear.
                Changes made:
                - Added a confirmation prompt in `clearBoard` using `window.confirm("Are you sure you want to clear the board?")`.
                - Clear action now aborts if user cancels.
                Result:
                - Board is only cleared after explicit user confirmation.

            [78] Request: Style the clear confirmation pop-up window.
                Changes made:
                - Replaced native browser confirm with a styled in-app modal overlay.
                - Added dedicated clear flow:
                    - `clearBoard` now opens modal,
                    - `performClearBoard` executes actual clear + broadcast + history.
                - Added modal actions:
                    - `Cancel` closes modal,
                    - `Yes, clear` confirms and clears board.
                Result:
                - Clear confirmation is now a styled popup consistent with board UI.

            [79] Request: Show active users per board on My Boards page and add Save progress button under Main menu.
                Changes made:
                - Added server API endpoint `GET /api/board-presence` that returns current live users per requested board ID.
                - Updated My Boards page to poll presence every 3s for listed board IDs and display:
                    - active user count,
                    - live user chips (emoji + nickname + color) on each board card.
                - Added manual `💾 Save progress` button under `🏠 Main menu` on board page.
                - Wired manual save to force immediate preview persistence and show a temporary `Saved` notice.
                Result:
                - My Boards now shows who is active on each board in real time.
                - Users can manually trigger board progress save from the board header.

            [80] Request: Make active users show who is on the board right now and ensure progress is saved even when no users are online.
                Changes made:
                - Updated My Boards active-user section wording to `Active right now` and show all currently online user chips for each board.
                - Added explicit `No users online right now` state when a board has zero active users.
                - Hardened server persistence on board leave: when the last user leaves a board, server now forces immediate persistence flush to disk.
                Result:
                - My Boards clearly shows exactly which users are currently on each board.
                - Board progress remains persisted even when everyone leaves the board.

            [81] Request: Add unit tests for key app functionality (progress persistence between sessions, max users per board, max boards in My Boards).
                Changes made:
                - Added test framework:
                    - installed `vitest`,
                    - added npm script: `test`.
                - Added shared server utility module `lib/server-utils.ts` for pure, testable logic:
                    - max-users join check,
                    - board persistence serialization,
                    - board persistence parsing.
                - Refactored `server.ts` to use shared utility functions for join-limit and persistence load/save conversion.
                - Added unit tests:
                    - `lib/server-utils.test.ts`:
                        - validates max 10 users rule (11th rejected),
                        - validates persistence round-trip for board actions/objects and omission of empty boards.
                    - `lib/boards.test.ts`:
                        - validates My Boards is capped at 5 entries when bulk saving,
                        - validates cap remains 5 during repeated upserts.
                Validation:
                - `npm test`: 4/4 tests passing.
                - `npm run lint`: passing.

            [82] Request: Fix active users showing 0 on My Boards while users are online; fix progress not saving between sessions; add unit tests.
                Changes made:
                - Added reusable presence utility (`buildPresenceSnapshot`) in `lib/server-utils.ts` and switched server presence API to use it.
                - Hardened My Boards presence polling:
                    - appended timestamp cache-buster query param,
                    - only updates state when a valid `presence` payload is returned.
                - Hardened persistence durability in `server.ts`:
                    - added backup file write (`boards.json.bak`) on persistence,
                    - added startup fallback load order: main file -> temp file -> backup file.
                - Added reusable persistence helper (`shouldFlushAfterLeave`) and used it in server leave flow.
                - Added unit tests in `lib/server-utils.test.ts` for:
                    - active-users presence snapshot correctness (non-zero users returned),
                    - flush-on-last-leave behavior,
                    - existing persistence round-trip and user-limit tests remain passing.
                Validation:
                - `npm test`: 6/6 tests passing.
                - `npm run lint`: passing.

            [83] Request: Simplify codebase and break into modules.
                Changes made:
                - Extracted Collaborative Board domain types into a dedicated module:
                    - `components/collaborative-board/types.ts`.
                - Extracted Collaborative Board pure/helper logic into a dedicated module:
                    - `components/collaborative-board/utils.ts`.
                - Refactored `components/CollaborativeBoard.tsx` to import from these modules instead of defining all types/helpers inline.
                - Preserved runtime behavior while reducing file complexity and improving separation of concerns.
                Validation:
                - `npm run lint`: passing.
                - `npm test`: 6/6 tests passing.

            [84] Request: Make code clearer.
                Changes made:
                - Extracted shape toolbar configuration and icon rendering into dedicated module:
                    - `components/collaborative-board/shape-config.tsx`.
                - Simplified `components/CollaborativeBoard.tsx` by replacing inline shape config/render logic with imports (`SHAPE_OPTIONS`, `getShapeLabel`, `renderShapeOutlineIcon`).
                - Extracted My Boards presence networking logic into dedicated module:
                    - `lib/board-presence.ts` (types + query builder + fetch helper).
                - Simplified `app/my-boards/page.tsx` by using `fetchBoardPresence` helper instead of inline request construction/parsing.
                - Added unit test for new presence query helper:
                    - `lib/board-presence.test.ts`.
                Validation:
                - `npm run lint`: passing.
                - `npm test`: 7/7 tests passing.

            [85] Request: Update code to production level.
                Changes made:
                - Added production-quality quality gates in `package.json` scripts:
                    - `typecheck` (`tsc --noEmit`),
                    - `check` (`lint + typecheck + test + build`).
                - Hardened presence networking in `lib/board-presence.ts`:
                    - added fetch timeout via `AbortController`,
                    - wrapped request in `try/catch/finally` for safe failure handling.
                - Added production-safe app fallbacks:
                    - `app/error.tsx` (global error boundary UI),
                    - `app/not-found.tsx` (404 UI).
                - Updated `README.md` with production workflow and readiness guidance.
                - Fixed strict typecheck issues surfaced by new gate.
                Validation:
                - `npm run check`: passing (lint + typecheck + tests + production build).

            [86] Request: Prepare for Vercel deployment.
                Changes made:
                - Added runtime backend URL config module:
                    - `lib/runtime-config.ts` (`NEXT_PUBLIC_BACKEND_URL` support).
                - Updated frontend networking to support split deployment (Vercel frontend + external realtime backend):
                    - `components/CollaborativeBoard.tsx` now connects Socket.IO via configurable backend URL.
                    - Added `connect_error` handling for clearer realtime connection failures.
                    - `lib/board-presence.ts` now fetches presence endpoint through configurable backend URL.
                - Added `.env.example` documenting `NEXT_PUBLIC_BACKEND_URL`.
                - Updated `README.md` with explicit Vercel deployment guide and architecture notes.
                Validation:
                - `npm run check`: passing (lint + typecheck + tests + production build).

            [87] Request: Fix My Boards showing 0 active users when there should be 1.
                Root cause:
                - Cross-origin fetch to `/api/board-presence` from Vercel frontend to external backend lacked CORS headers, causing browser-blocked responses and fallback `0` display.
                Changes made:
                - Added CORS headers for `/api/board-presence` responses in `server.ts`.
                - Added `OPTIONS` handling for `/api/board-presence` (204) for robust cross-origin behavior.
                Validation:
                - `npm run lint`: passing.
                - `npm run test`: 7/7 passing.

            [88] Request: Active users still shows 0 while board has 1 online user.
                Changes made:
                - Added local-presence heartbeat fallback in `lib/board-presence.ts`:
                    - `upsertLocalBoardPresence`,
                    - `removeLocalBoardPresence`,
                    - `mergeBoardPresenceWithLocal`.
                - Updated board runtime (`components/CollaborativeBoard.tsx`) to write/remove local heartbeat for current connected user every 2s.
                - Updated My Boards page (`app/my-boards/page.tsx`) to merge remote presence with local heartbeat fallback.
                - Added unit test coverage in `lib/board-presence.test.ts` for local fallback merge behavior.
                Validation:
                - `npm run lint`: passing.
                - `npm run test`: 8/8 passing.

            [89] Request: Progress still not restored between sessions after reopening board.
                Root cause:
                - In some deployments, board can reopen with empty server state (cold-start/ephemeral state timing), resulting in blank canvas even though prior session had drawings.
                Changes made:
                - Added local full-board snapshot persistence in `components/CollaborativeBoard.tsx` during preview persistence flow.
                - Added restore fallback on join:
                    - when server returns empty state, board attempts restore from local snapshot,
                    - restored snapshot is immediately emitted via `replace-canvas` to sync backend state.
                Result:
                - Reopened board now restores visible progress even if server returns empty state at join.
                Validation:
                - `npm run lint`: passing.
                - `npm run test`: 8/8 passing.
