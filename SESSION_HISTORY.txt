CollBrush Session History
Date: 2026-03-04

[1] Request: Build a real-time collaborative paint app (Next.js + TypeScript), multi-user like Miro, max 10 concurrent users per session, multi-session boards, responsive and clean UI.
    Changes made:
    - Scaffolded Next.js (App Router, TypeScript, Tailwind).
    - Added Socket.IO server and realtime board rooms.
    - Implemented max 10 users per board.
    - Added multi-board routing and board create/join flow.
    - Built collaborative canvas UI with pen/eraser/colors/size/clear/copy-link.
    - Updated metadata, styles, scripts, and README.
    - Verified with lint/build and runtime startup.

[2] Request: Add a txt file to store session history and update after every change.
    Changes made:
    - Created this file: SESSION_HISTORY.txt
    - Added initial full history entries for this session.
    - Going forward, this file will be appended after each code/content change in our session.

[3] Request: "project doesnt launch"
    Investigation:
    - Reproduced startup behavior and observed intermittent Next.js lock conflict.
    - Error captured: unable to acquire .next/dev/lock because another dev instance was active.
    Fixes made:
    - Killed duplicate/stale dev instance.
    - Added automatic stale lock cleanup in npm dev script before starting server.
    Result:
    - `npm run dev` launches successfully and serves on port 3000.
